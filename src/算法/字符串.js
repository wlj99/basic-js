// 字符串 利用数组的reverse 
function reverse(str) {
  return str.split('').reverse().join('');
}

// 输入一个字符串，有加减乘除，整数，和小数，得出最后的结果
function operation(str) {
  let res = [];
  let lastIndex = 0;
  let currIndex = 0;

}